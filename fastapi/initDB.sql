use diancan;

CREATE TABLE goods (
    id INT PRIMARY KEY,
    name VARCHAR(255),
    price INT,
    discount INT,
    kind VARCHAR(255),
    tag VARCHAR(255),
    opts VARCHAR(255),
    img VARCHAR(255)
);
